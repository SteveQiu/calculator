<?xml version="1.0" encoding="utf-8"?>
<!--
    Bottom-sheet style unlock dialog shown when a user taps a locked theme.
    Host DialogFragment should set the theme name and preview colors
    programmatically via view IDs.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CC000000">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="@color/colorBackground"
        app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

        <!-- Drag handle -->
        <View
            android:layout_width="40dp"
            android:layout_height="4dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="8dp"
            android:background="#44808080"
            android:backgroundTintMode="src_in" />

        <!-- Header row: theme name + close button -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp">

            <TextView
                android:id="@+id/tvDialogThemeName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical|start"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@color/colorTextPrimary" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnClose"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="center_vertical|end"
                android:text="✕"
                android:textSize="18sp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:padding="0dp"
                app:cornerRadius="24dp"
                style="@style/Widget.Calculator.Button.Ghost" />

        </FrameLayout>

        <!-- Subtitle -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingBottom="16dp"
            android:text="@string/unlock_theme"
            android:textSize="14sp"
            android:textColor="@color/colorTextSecondary" />

        <!-- Theme color preview -->
        <FrameLayout
            android:id="@+id/themePreviewContainer"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="20dp"
            android:background="@drawable/bg_theme_preview_dialog">

            <!-- Three large colored blocks representing the palette -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:weightSum="3">

                <View
                    android:id="@+id/previewBlockSpecial"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1" />

                <View
                    android:id="@+id/previewBlockNumber"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1" />

                <View
                    android:id="@+id/previewBlockOperator"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1" />

            </LinearLayout>
        </FrameLayout>

        <!-- Watch Ad button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnWatchAd"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="10dp"
            android:text="▶  @string/watch_ad_to_unlock"
            android:textSize="15sp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:cornerRadius="26dp"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

        <!-- Buy button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBuy"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="10dp"
            android:text="@string/buy_to_unlock"
            android:textSize="15sp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:backgroundTint="@color/colorBtnOperator"
            android:textColor="@color/colorTextOnOperator"
            app:cornerRadius="26dp"
            style="@style/Widget.Calculator.Button" />

        <!-- Restore purchase link -->
        <TextView
            android:id="@+id/tvRestorePurchase"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="32dp"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:text="@string/restore_purchase"
            android:textSize="13sp"
            android:textColor="@color/colorTextSecondary"
            android:textAlignment="center"
            android:layout_marginTop="4dp" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
